---
phase: 03-dual-mode-routing
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/components/nav/PaywallOverlay.astro
  - src/components/nav/AudioGuideOverlay.astro
  - src/components/nav/NavProfileButton.astro
  - src/components/nav/ProfileOverlay.astro
  - src/components/sections/ArtworkSlider.astro
  - src/components/nav/HamburgerOverlay.astro
  - src/components/nav/NavBar.astro
  - src/layouts/BaseLayout.astro
  - src/content/shared/ui-strings.json
autonomous: true
requirements: [NAV-02, NAV-03]

must_haves:
  truths:
    - "Tapping headphones on an artwork while logged out opens the PaywallOverlay"
    - "Clicking Buy Tickets or Become a Member in paywall simulates login and closes paywall"
    - "After login, profile icon appears in the navbar; hamburger Log in becomes My Account"
    - "Tapping headphones while logged in opens AudioGuideOverlay in Playlist view"
    - "Tapping a track in the playlist slides up the DetailPlayer bottom-sheet with transport controls"
    - "Tapping the profile icon opens ProfileOverlay with QR ticket, 6 nav items, and log out"
    - "Logging out restores the pre-login state (profile icon gone, Log in reappears)"
  artifacts:
    - path: "src/components/nav/PaywallOverlay.astro"
      provides: "Dismissible paywall modal with Buy Tickets + Become a Member CTAs"
    - path: "src/components/nav/AudioGuideOverlay.astro"
      provides: "Fullscreen overlay with Playlist + DetailPlayer bottom-sheet views"
    - path: "src/components/nav/NavProfileButton.astro"
      provides: "Conditional profile icon in navbar"
    - path: "src/components/nav/ProfileOverlay.astro"
      provides: "Logged-in profile panel with QR ticket + nav items + log out"
  key_links:
    - from: "src/components/sections/ArtworkSlider.astro"
      to: "window.__paywall / window.__audioGuideOverlay"
      via: "headphones button click handler checks window.__demoUser"
      pattern: "__demoUser.*__paywall|__audioGuideOverlay"
    - from: "src/components/nav/PaywallOverlay.astro"
      to: "localStorage kunsthaus-demo-user"
      via: "simulateLogin() sets flag, dispatches kunsthaus:login"
      pattern: "kunsthaus-demo-user|kunsthaus:login"
    - from: "src/components/nav/NavBar.astro"
      to: "src/components/nav/NavProfileButton.astro"
      via: "kunsthaus:login event listener shows profile button"
      pattern: "kunsthaus:login"
---

<objective>
Wire the audio guide, paywall, login simulation, and profile system into the existing KJM detail page and global navigation. This completes Phase 03 Success Criteria 5-8: headphones trigger paywall or audio guide depending on login state, login simulation updates the navbar and hamburger, the audio guide has Playlist + DetailPlayer views, and the profile overlay shows a QR ticket.

Purpose: This is the final plan of Phase 03. It adds the interactive layer that makes the on-site experience feel like a real museum app -- the "aha moment" for the competition pitch.

Output: 4 new components (PaywallOverlay, AudioGuideOverlay, NavProfileButton, ProfileOverlay) + wiring into ArtworkSlider, NavBar, HamburgerOverlay, and BaseLayout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-dual-mode-routing/03-CONTEXT.md
@.planning/phases/03-dual-mode-routing/03-03-SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs from existing code -->

From src/layouts/BaseLayout.astro:
- Overlays are rendered between NavBar and <main>: SearchOverlay, HamburgerOverlay, HoursOverlay
- All overlays expose open/close on window.__overlayName
- Lenis exposed as window.__lenis for scroll lock
- Mode prop: mode?: 'planning' | 'onsite' (default 'planning')
- Import pattern: import Component from '../components/nav/Component.astro';

From src/components/nav/HamburgerOverlay.astro:
- Glass backdrop: color-mix(in srgb, var(--color-surface) 85%, transparent) + backdrop-filter: blur(24px)
- z-index: 80 for all overlays
- Dual-init: DOMContentLoaded + astro:page-load
- Expose open/close on window.__hamburgerOverlay
- Secondary nav has "Log in" item at line 106 (href="${base}/login", text "Log in"/"Anmelden")
- Scroll lock: window.__lenis?.stop() on open, window.__lenis?.start() on close

From src/components/sections/ArtworkSlider.astro:
- Headphones button at lines 88-94: class="artwork-slider__headphones", aria-label="Play audio guide for {title}"
- Currently has NO click handler (silent stub per 03-03)
- Icon: <span class="icon" aria-hidden="true">headphones</span>
- Each slide has data: art.id, art.audioSrc, art.title[lang]

From src/components/nav/NavBar.astro:
- navbar__right contains hamburger button only
- z-index: 60
- Dual-init pattern with initNavBar() + astro:page-load
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PaywallOverlay + AudioGuideOverlay + simulateLogin core</name>
  <files>
    src/components/nav/PaywallOverlay.astro
    src/components/nav/AudioGuideOverlay.astro
    src/content/shared/ui-strings.json
  </files>
  <read_first>
    src/components/nav/HamburgerOverlay.astro
    src/components/nav/HoursOverlay.astro
    src/components/nav/SearchOverlay.astro
    src/layouts/BaseLayout.astro
    src/content/shared/ui-strings.json
    src/content/onsite/kerry-james-marshall.json
    .planning/phases/03-dual-mode-routing/03-CONTEXT.md
  </read_first>
  <action>
Create two new overlay components following the established overlay pattern (glass backdrop, z-index 80, scroll lock via window.__lenis, dual-init with DOMContentLoaded + astro:page-load).

**PaywallOverlay.astro** (`src/components/nav/PaywallOverlay.astro`):
- Fullscreen overlay (same glass backdrop pattern as HamburgerOverlay: `color-mix(in srgb, var(--color-surface) 85%, transparent)` + `backdrop-filter: blur(24px)`, `position: fixed; inset: 0; z-index: 80`)
- Dismissible via X button AND backdrop click
- Content centered vertically + horizontally in a card container (max-width: 420px, semantic token background):
  - Heading: "Audio guide access" / "Audioguide" (localized, read from ui-strings or inline)
  - Body paragraph (per CONTEXT.md Paywall overlay copy): English = "The audio guide is free when you buy your ticket online. If you purchased your ticket at the counter, you can rent the audio guide for CHF 4 on site. Members of Kunsthaus Zurich always listen free." German equivalent also supported.
  - Two CTA buttons stacked: "Buy tickets" (primary, class `btn hover-wipe touch-pulse touch-target`) and "Become a member" (secondary, outlined variant)
  - Both CTAs call `simulateLogin()` which: (1) sets `localStorage.setItem('kunsthaus-demo-user', 'purchased')`, (2) sets `window.__demoUser = 'purchased'`, (3) dispatches `window.dispatchEvent(new CustomEvent('kunsthaus:login'))`, (4) closes the paywall overlay
- Expose on `window.__paywall = { open, close }`
- `open()` accepts an optional `trackId` string; stores it on `window.__paywall._pendingTrackId` so after login the audio guide can auto-open to that track
- Use `data-lenis-prevent` on the scrollable content area

**AudioGuideOverlay.astro** (`src/components/nav/AudioGuideOverlay.astro`):
- Fullscreen overlay, same glass backdrop + z-index 80
- Two-layer internal structure:
  1. **Playlist view** (base layer, always visible when overlay is open):
     - Header with back button: icon `arrow_back` + text "Close audio guide" / "Audioguide schliessen"
     - List of tracks from `data-tracks` JSON attribute (stringified artwork array). Each track row: number (01-10), title, year, play button icon. Tapping a row calls `openDetailPlayer(trackId)`.
     - Currently playing track highlighted with an `is-playing` class
  2. **DetailPlayer view** (bottom-sheet layer, slides up from below via GSAP translateY):
     - Starts at `transform: translateY(100%)` (hidden below viewport)
     - `openDetailPlayer()` animates to `translateY(0)` via `gsap.to(detailEl, { y: '0%', duration: 0.4, ease: 'power3.out' })`
     - Slide-down: tapping the chevron-down handle (`keyboard_arrow_down` icon, large 48px tap-target at top) animates back to `translateY(100%)`
     - Content: artwork image (large), title, year, collection, description
     - Transport controls: previous (`skip_previous`), play/pause toggle (`play_arrow` / `pause`), next (`skip_next`), progress bar (HTML `<input type="range">` synced to audio currentTime/duration)
     - Actual `<audio>` element: `const audio = new Audio()` created once in the init closure, persists across track changes. `audio.src` set from artwork's `audioSrc` field. Play/pause toggles `audio.play()` / `audio.pause()`. `timeupdate` event updates the range slider.
- On `close()`: pause audio, reset detail player to translateY(100%), hide overlay
- Expose on `window.__audioGuideOverlay = { open(trackId?), close }`
- `open(trackId?)` shows the playlist; if `trackId` is provided, immediately opens detail player for that track and starts playback
- Use Material Symbols icons throughout: `arrow_back`, `keyboard_arrow_down`, `skip_previous`, `play_arrow`, `pause`, `skip_next`, `headphones`
- Reduce-motion: skip GSAP bottom-sheet animation, snap to final position

Add ui-strings entries to `src/content/shared/ui-strings.json` for: `audio-guide-close` ("Close audio guide" / "Audioguide schliessen"), `paywall-heading` ("Audio guide access" / "Audioguide"), `paywall-buy` ("Buy tickets" / "Tickets kaufen"), `paywall-member` ("Become a member" / "Mitglied werden").
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && grep -c 'window.__paywall' src/components/nav/PaywallOverlay.astro && grep -c 'window.__audioGuideOverlay' src/components/nav/AudioGuideOverlay.astro && grep -c 'kunsthaus-demo-user' src/components/nav/PaywallOverlay.astro && grep -c 'kunsthaus:login' src/components/nav/PaywallOverlay.astro && grep -c 'translateY' src/components/nav/AudioGuideOverlay.astro && grep -c 'new Audio' src/components/nav/AudioGuideOverlay.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'window.__paywall' src/components/nav/PaywallOverlay.astro` returns matches for both `open` and `close`
    - `grep 'kunsthaus-demo-user' src/components/nav/PaywallOverlay.astro` returns >= 1 match (localStorage set)
    - `grep 'kunsthaus:login' src/components/nav/PaywallOverlay.astro` returns >= 1 match (CustomEvent dispatch)
    - `grep 'window.__audioGuideOverlay' src/components/nav/AudioGuideOverlay.astro` returns matches for both `open` and `close`
    - `grep 'translateY' src/components/nav/AudioGuideOverlay.astro` returns >= 2 matches (show + hide animation)
    - `grep 'new Audio' src/components/nav/AudioGuideOverlay.astro` returns >= 1 match (persistent audio element)
    - `grep 'data-lenis-prevent' src/components/nav/PaywallOverlay.astro` returns >= 1 match
    - `grep 'astro:page-load' src/components/nav/PaywallOverlay.astro` returns >= 1 match (dual-init)
    - `grep 'astro:page-load' src/components/nav/AudioGuideOverlay.astro` returns >= 1 match (dual-init)
    - `grep 'audio-guide-close' src/content/shared/ui-strings.json` returns >= 1 match
    - `grep 'paywall-heading' src/content/shared/ui-strings.json` returns >= 1 match
    - PaywallOverlay uses `z-index: 80` or higher (matching other overlays)
    - AudioGuideOverlay uses `z-index: 80` or higher
    - Both components use `var(--color-surface)` and `var(--color-text)` (no hardcoded hex except acceptable dark-only values)
    - Both components use `backdrop-filter: blur` pattern
  </acceptance_criteria>
  <done>PaywallOverlay and AudioGuideOverlay exist with full markup, styles, and scripts. simulateLogin() is implemented inside PaywallOverlay. Audio playback works via persistent Audio element. DetailPlayer bottom-sheet animates via GSAP translateY. Both overlays follow the established dual-init + glass backdrop + Lenis scroll lock pattern.</done>
</task>

<task type="auto">
  <name>Task 2: NavProfileButton + ProfileOverlay + login-aware HamburgerOverlay</name>
  <files>
    src/components/nav/NavProfileButton.astro
    src/components/nav/ProfileOverlay.astro
    src/components/nav/HamburgerOverlay.astro
    src/components/nav/NavBar.astro
  </files>
  <read_first>
    src/components/nav/NavBar.astro
    src/components/nav/HamburgerOverlay.astro
    src/components/nav/HoursOverlay.astro
    src/layouts/BaseLayout.astro
    .planning/phases/03-dual-mode-routing/03-CONTEXT.md
  </read_first>
  <action>
**NavProfileButton.astro** (`src/components/nav/NavProfileButton.astro`):
- A `<button>` with Material Symbols icon `account_circle` (24x24), class `navbar__icon-btn touch-target touch-pulse`
- Hidden by default (`display: none`). On `kunsthaus:login` event OR if `localStorage.getItem('kunsthaus-demo-user')` exists on init, set `display: flex` with a GSAP fade-in (`gsap.fromTo(el, { opacity: 0, scale: 0.8 }, { opacity: 1, scale: 1, duration: 0.4, ease: 'back.out(1.7)' })`)
- On `kunsthaus:logout` event, GSAP fade-out then `display: none`
- Click opens `window.__profileOverlay?.open()`
- Dual-init pattern (DOMContentLoaded + astro:page-load, guarded by data-initialized)
- Expose nothing on window (it's reactive to events, not called externally)

**NavBar.astro modification** (`src/components/nav/NavBar.astro`):
- Import NavProfileButton: `import NavProfileButton from './NavProfileButton.astro';`
- Insert `<NavProfileButton />` inside `.navbar__right`, BEFORE the hamburger button. The profile icon sits to the left of the hamburger icon.
- No other changes to NavBar markup or styles needed. The existing `.navbar__right` is `display: flex; align-items: center;` which already handles multiple children. Add `gap: var(--space-1)` to `.navbar__right` to space the two buttons.

**ProfileOverlay.astro** (`src/components/nav/ProfileOverlay.astro`):
- Same glass backdrop overlay pattern (z-index 80, blur, scroll lock)
- Header: back button (icon `arrow_back` + "Profile" text) + close X button
- Body (scrollable, `data-lenis-prevent`):
  1. **QR Ticket Hero card**: bordered card at top containing:
     - Heading: "Day Pass -- All Exhibitions" / "Tagespass -- Alle Ausstellungen"
     - QR code: render as a simple placeholder `<div class="profile-overlay__qr">` with a centered `qr_code_2` Material Symbol icon at 120px (real QR generation is out of scope). Below QR: text "KUNSTHAUS-DEMO-2026-04-22-DAYPASS-001"
     - Valid: "22 April 2026, 10:00-20:00"
     - Access list: "All current exhibitions + permanent collection"
  2. **Secondary nav** (6 items, each a row with label + `arrow_forward` icon):
     - "My audio guide history" / "Mein Audioguide-Verlauf"
     - "Favourites" / "Favoriten"
     - "Upcoming visits & events" / "Geplante Besuche & Events"
     - "Membership" / "Mitgliedschaft"
     - "Account settings" / "Kontoeinstellungen"
     - "Help & contact" / "Hilfe & Kontakt"
     All are `<a href="#">` stubs (no real pages exist)
  3. **Footer**: "Signed in as Demo User" + "Log out" button
     - Log out calls: `localStorage.removeItem('kunsthaus-demo-user')`, `window.__demoUser = null`, `window.dispatchEvent(new CustomEvent('kunsthaus:logout'))`, closes overlay
  4. **Optional upsell card** at very bottom: "Become a member" card with brief pitch + CTA link
- Expose on `window.__profileOverlay = { open, close }`

**HamburgerOverlay.astro modification** (`src/components/nav/HamburgerOverlay.astro`):
- Find the secondary nav `<li>` for "Log in" (line ~106, the one with `href={base}/login` and text "Log in" / "Anmelden")
- Add `data-login-item` attribute to that `<li>` so JS can find it
- In the init script, add login-state awareness:
  - On init: check `localStorage.getItem('kunsthaus-demo-user')`. If set, replace the "Log in" link text with "My Account" / "Mein Konto" and change href to `#` with a click handler that opens `window.__profileOverlay?.open()` and closes hamburger
  - Listen for `kunsthaus:login` event: do the same replacement
  - Listen for `kunsthaus:logout` event: restore "Log in" text and original href
- Keep all existing functionality untouched (mode toggle, language switcher, search/hours swaps)
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && grep -c 'NavProfileButton' src/components/nav/NavBar.astro && grep -c 'account_circle' src/components/nav/NavProfileButton.astro && grep -c 'window.__profileOverlay' src/components/nav/ProfileOverlay.astro && grep -c 'kunsthaus:login' src/components/nav/NavProfileButton.astro && grep -c 'kunsthaus:logout' src/components/nav/ProfileOverlay.astro && grep -c 'data-login-item' src/components/nav/HamburgerOverlay.astro && grep -c 'kunsthaus:login' src/components/nav/HamburgerOverlay.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import NavProfileButton' src/components/nav/NavBar.astro` returns 1 match
    - `grep '<NavProfileButton' src/components/nav/NavBar.astro` returns 1 match, appearing inside `.navbar__right` before the hamburger button
    - `grep 'account_circle' src/components/nav/NavProfileButton.astro` returns >= 1 match
    - `grep 'kunsthaus:login' src/components/nav/NavProfileButton.astro` returns >= 1 match (listens for login event)
    - `grep 'kunsthaus:logout' src/components/nav/NavProfileButton.astro` returns >= 1 match (listens for logout event)
    - `grep 'window.__profileOverlay' src/components/nav/ProfileOverlay.astro` returns >= 1 match with `open` and `close`
    - `grep 'kunsthaus:logout' src/components/nav/ProfileOverlay.astro` returns >= 1 match (dispatches logout)
    - `grep 'qr_code_2' src/components/nav/ProfileOverlay.astro` returns >= 1 match (QR placeholder icon)
    - `grep 'data-login-item' src/components/nav/HamburgerOverlay.astro` returns >= 1 match
    - `grep 'kunsthaus:login' src/components/nav/HamburgerOverlay.astro` returns >= 1 match (new listener)
    - `grep 'My Account\|Mein Konto' src/components/nav/HamburgerOverlay.astro` returns >= 1 match
    - ProfileOverlay contains all 6 secondary nav labels (grep for "audio guide history" or "Favourites")
    - NavProfileButton uses `display: none` as default state
    - `.navbar__right` has `gap: var(--space-1)` added
  </acceptance_criteria>
  <done>NavProfileButton appears in navbar when logged in and hides on logout. ProfileOverlay opens from profile button with QR ticket, 6 nav items, and log out. HamburgerOverlay "Log in" dynamically becomes "My Account" when logged in and reverts on logout. All login/logout state changes propagate via CustomEvent.</done>
</task>

<task type="auto">
  <name>Task 3: Wire overlays into BaseLayout + ArtworkSlider headphones + build verification</name>
  <files>
    src/layouts/BaseLayout.astro
    src/components/sections/ArtworkSlider.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro
    src/components/sections/ArtworkSlider.astro
    src/components/nav/PaywallOverlay.astro
    src/components/nav/AudioGuideOverlay.astro
    src/components/nav/ProfileOverlay.astro
    src/pages/onsite/kerry-james-marshall.astro
    src/content/onsite/kerry-james-marshall.json
  </read_first>
  <action>
**BaseLayout.astro modification** (`src/layouts/BaseLayout.astro`):
- Import the 3 new overlays after the existing overlay imports (after HoursOverlay import):
  ```
  import PaywallOverlay from '../components/nav/PaywallOverlay.astro';
  import AudioGuideOverlay from '../components/nav/AudioGuideOverlay.astro';
  import ProfileOverlay from '../components/nav/ProfileOverlay.astro';
  ```
- Render them after HoursOverlay, before `<main>`:
  ```
  <PaywallOverlay lang={lang} />
  <AudioGuideOverlay lang={lang} />
  <ProfileOverlay lang={lang} />
  ```
- Add a small inline script block (before the existing Lenis script) that initializes `window.__demoUser` from localStorage on page load:
  ```
  <script is:inline>
    window.__demoUser = localStorage.getItem('kunsthaus-demo-user') || null;
  </script>
  ```
  This must be `is:inline` so it runs synchronously before any component scripts check `window.__demoUser`. It must also re-run on `astro:page-load` to survive View Transitions — add an event listener inside the same script block.

**ArtworkSlider.astro modification** (`src/components/sections/ArtworkSlider.astro`):
- In the existing `initArtworkSlider()` function, add a click handler for the headphones buttons.
- Inside the `slider.addEventListener('click', ...)` block (or add a new delegated listener), detect clicks on `.artwork-slider__headphones`:
  ```typescript
  const headphonesBtn = (e.target as HTMLElement).closest('.artwork-slider__headphones');
  if (headphonesBtn) {
    const slide = headphonesBtn.closest('.artwork-slider__slide');
    const trackId = slide?.dataset.trackId;
    if (!(window as any).__demoUser) {
      // Not logged in — open paywall, store pending track
      (window as any).__paywall?.open(trackId);
    } else {
      // Logged in — open audio guide directly
      (window as any).__audioGuideOverlay?.open(trackId);
    }
    return;
  }
  ```
- Add `data-track-id={art.id}` attribute to each `.artwork-slider__slide` `<li>` element (in the Astro template markup) so the JS can identify which track was tapped.
- Also listen for `kunsthaus:login` to auto-open the audio guide if there was a pending track:
  ```typescript
  window.addEventListener('kunsthaus:login', () => {
    const pendingTrack = (window as any).__paywall?._pendingTrackId;
    if (pendingTrack) {
      (window as any).__paywall._pendingTrackId = null;
      // Small delay to let paywall close animation finish
      setTimeout(() => {
        (window as any).__audioGuideOverlay?.open(pendingTrack);
      }, 400);
    }
  });
  ```

**Build verification:**
After both modifications, run `npm run build` from the project root. Confirm:
1. Build succeeds with 3 pages (index, onsite/index, onsite/kerry-james-marshall)
2. No new Zod schema errors
3. `grep -rn '__paywall\|__audioGuideOverlay\|__profileOverlay' dist/onsite/kerry-james-marshall/index.html` returns matches (overlays are rendered in the built output since they're in BaseLayout)
4. `grep -c 'data-track-id' src/components/sections/ArtworkSlider.astro` returns >= 1
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && grep -c 'PaywallOverlay' src/layouts/BaseLayout.astro && grep -c 'AudioGuideOverlay' src/layouts/BaseLayout.astro && grep -c 'ProfileOverlay' src/layouts/BaseLayout.astro && grep -c 'window.__demoUser' src/layouts/BaseLayout.astro && grep -c 'data-track-id' src/components/sections/ArtworkSlider.astro && grep -c '__paywall' src/components/sections/ArtworkSlider.astro && grep -c '__audioGuideOverlay' src/components/sections/ArtworkSlider.astro && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'import PaywallOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep 'import AudioGuideOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep 'import ProfileOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep '<PaywallOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep '<AudioGuideOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep '<ProfileOverlay' src/layouts/BaseLayout.astro` returns 1 match
    - `grep 'window.__demoUser' src/layouts/BaseLayout.astro` returns >= 1 match
    - `grep 'is:inline' src/layouts/BaseLayout.astro` returns >= 1 match (inline script for __demoUser)
    - `grep 'data-track-id' src/components/sections/ArtworkSlider.astro` returns >= 1 match
    - `grep '__paywall' src/components/sections/ArtworkSlider.astro` returns >= 1 match
    - `grep '__audioGuideOverlay' src/components/sections/ArtworkSlider.astro` returns >= 1 match
    - `grep 'kunsthaus:login' src/components/sections/ArtworkSlider.astro` returns >= 1 match (auto-open after login)
    - `npm run build` exits 0 with 3 pages built
    - No new content sync errors in build output
  </acceptance_criteria>
  <done>All 4 new overlays rendered in BaseLayout on every page. ArtworkSlider headphones buttons are wired: logged-out opens paywall, logged-in opens audio guide. After paywall login, audio guide auto-opens to the pending track. Build succeeds with all 3 pages generated. Phase 03 SCs 5-8 are structurally complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | Demo-only login state; no real auth |
| window globals | Overlay coordination via window.__* |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | simulateLogin | accept | Demo-only login simulation via localStorage; no real credentials, no real auth. Acceptable for a competition prototype. |
| T-03-02 | Tampering | localStorage kunsthaus-demo-user | accept | Any user can set/clear the flag in devtools. This is intentional for a demo -- no real access control. |
| T-03-03 | Information Disclosure | Audio file URLs | accept | Audio URLs are public static files. No gating of actual audio content (paywall is UX demo only). |
</threat_model>

<verification>
1. `npm run build` succeeds with 3 pages
2. On `/onsite/kerry-james-marshall`: tapping headphones while not logged in shows PaywallOverlay
3. Clicking "Buy tickets" in paywall: localStorage set, paywall closes, profile icon appears in navbar, audio guide opens
4. Audio guide shows playlist of 10 tracks; tapping a track slides up DetailPlayer with transport controls
5. Tapping profile icon opens ProfileOverlay with QR ticket placeholder, 6 nav items, and log out
6. Logging out removes profile icon, restores "Log in" in hamburger
7. Opening hamburger while logged in shows "My Account" instead of "Log in"
8. All overlays dismiss on Escape key and/or X button
9. All overlays use semantic tokens (no hardcoded colors that break the dark cascade)
10. prefers-reduced-motion: bottom-sheet snaps instead of animating
</verification>

<success_criteria>
Phase 03 Success Criteria 5-8 all pass structurally:
- SC5: Headphones -> paywall (logged out) with Buy Tickets + Become a Member CTAs
- SC6: Buy Tickets simulates login; profile icon fades into navbar; hamburger "Log in" becomes "My Account"
- SC7: Audio guide overlay opens with Playlist view; tapping track slides up DetailPlayer bottom-sheet with transport controls
- SC8: Profile overlay shows QR ticket, 6 secondary nav items, and log out
Build succeeds. No regressions to SCs 1-4.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dual-mode-routing/03-04-SUMMARY.md`
</output>
