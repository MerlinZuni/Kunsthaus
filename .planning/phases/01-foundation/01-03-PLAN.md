---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/content.config.ts
  - src/content/planning/homepage.json
  - src/content/onsite/kerry-james-marshall.json
  - src/content/shared/navigation.json
  - src/pages/index.astro
autonomous: true
requirements:
  - TECH-02

must_haves:
  truths:
    - "Page content (text, image paths) is pulled from JSON data files, not hardcoded in templates"
    - "Content collections have Zod schema validation with typed fields"
    - "JSON content follows i18n structure with DE and EN, FR optional placeholder"
    - "Planning mode content follows the priority hierarchy from the brief"
    - "On-site content features Kerry James Marshall exhibition"
    - "Build fails if JSON content violates the schema (missing required fields)"
  artifacts:
    - path: "src/content.config.ts"
      provides: "Collection definitions with Zod schemas"
      contains: "defineCollection"
    - path: "src/content/planning/homepage.json"
      provides: "Planning mode homepage content (DE+EN)"
      contains: "hero"
    - path: "src/content/onsite/kerry-james-marshall.json"
      provides: "On-site exhibition content"
      contains: "Kerry James Marshall"
    - path: "src/content/shared/navigation.json"
      provides: "Shared navigation strings"
      contains: "navigation"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/planning/homepage.json"
      via: "file() loader reference"
      pattern: "file.*planning.*homepage"
    - from: "src/content.config.ts"
      to: "src/content/onsite/kerry-james-marshall.json"
      via: "file() loader reference"
      pattern: "file.*onsite.*kerry"
    - from: "src/pages/index.astro"
      to: "src/content.config.ts"
      via: "getCollection() query"
      pattern: "getCollection"
---

<objective>
Build the JSON content layer with Astro Content Collections, Zod schemas, and i18n structure. Populate with real-ish mock content for Planning mode (homepage sections from kunsthaus.ch) and On-site mode (Kerry James Marshall exhibition). Wire content into index page to prove the data flows from JSON to rendered HTML.

Purpose: Establish the CMS-ready content architecture so Phase 2 components never hardcode text -- all content comes from typed JSON files.
Output: Three content collections (planning, onsite, shared) with schema validation and content rendering on the index page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- BaseLayout and CSS tokens available -->

BaseLayout.astro provides:
- `<html lang="de" data-mode="planning">`
- `<main id="main"><slot /></main>`
- All CSS tokens available (--color-*, --space-*, --type-*)

Astro Content Collections API (Astro 6):
```typescript
import { defineCollection } from 'astro:content';
import { file } from 'astro/loaders';
import { z } from 'astro/zod';

// file() loader expects JSON that is either:
// - Array with id field: [{id: "...", ...}, ...]
// - Object with keys as IDs: {"key1": {...}, "key2": {...}}
```

Usage in pages:
```typescript
import { getCollection } from 'astro:content';
const entries = await getCollection('collectionName');
// entries[].data contains typed content
```
</interfaces>
</context>

<threat_model>
ASVS Level 1 assessment:
- Content is static JSON rendered at build time -- no runtime injection risk
- No user-submitted content -- all mock data is developer-authored
- Zod schema validation catches malformed data at build time
- No external API calls for content -- fully local JSON files
- Content scraping from kunsthaus.ch is manual copy-paste, not automated
No high-severity threats identified for this plan.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create content collection schemas and JSON data files</name>
  <files>
    src/content.config.ts, src/content/planning/homepage.json, src/content/onsite/kerry-james-marshall.json, src/content/shared/navigation.json
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Content Collections, Pitfall 3: file() loader JSON shape, i18n JSON example)
    .planning/phases/01-foundation/01-CONTEXT.md (D-11 through D-19 for content structure decisions)
  </read_first>
  <action>
    1. Create src/content.config.ts with three collections:

       ```typescript
       import { defineCollection } from 'astro:content';
       import { file } from 'astro/loaders';
       import { z } from 'astro/zod';

       // Reusable i18n text schema -- per D-12: DE+EN active, FR placeholder
       const localizedText = z.object({
         de: z.string(),
         en: z.string(),
         fr: z.string().optional(),
       });

       // Planning homepage sections -- per D-13 priority hierarchy
       const planningHomepage = defineCollection({
         loader: file("src/content/planning/homepage.json"),
         schema: z.object({
           id: z.string(),
           section: z.enum(["hero", "key-info", "exhibitions", "offerings", "events", "planning-details", "education"]),
           priority: z.number().int().min(1).max(10),
           title: localizedText,
           subtitle: localizedText.optional(),
           body: localizedText.optional(),
           image: z.string().optional(),
           items: z.array(z.object({
             id: z.string(),
             title: localizedText,
             description: localizedText.optional(),
             image: z.string().optional(),
             date: z.string().optional(),
             link: z.string().optional(),
           })).optional(),
         }),
       });

       // On-site exhibition content -- per D-15, D-16, D-19
       const onsiteExhibition = defineCollection({
         loader: file("src/content/onsite/kerry-james-marshall.json"),
         schema: z.object({
           id: z.string(),
           title: localizedText,
           artist: z.string(),
           dates: z.object({
             start: z.string(),
             end: z.string(),
           }),
           description: localizedText,
           artworks: z.array(z.object({
             id: z.string(),
             title: localizedText,
             description: localizedText,
             image: z.string(),
             room: z.string(),
             year: z.string().optional(),
             medium: localizedText.optional(),
             audioGuide: z.boolean().default(false),
             membersOnly: z.boolean().default(false),
           })),
           practicalInfo: z.object({
             rooms: z.array(z.string()),
             audioGuideAvailable: z.boolean(),
             estimatedDuration: localizedText,
           }).optional(),
         }),
       });

       // Shared UI strings -- navigation, common labels
       const shared = defineCollection({
         loader: file("src/content/shared/navigation.json"),
         schema: z.object({
           id: z.string(),
           label: localizedText,
           href: z.string().optional(),
           icon: z.string().optional(),
         }),
       });

       export const collections = {
         planning: planningHomepage,
         onsite: onsiteExhibition,
         shared,
       };
       ```

    2. Create src/content/planning/homepage.json -- Planning mode homepage content per D-13 priority hierarchy. Use content modeled after kunsthaus.ch (per D-14). This is mock data but should feel realistic:

       ```json
       [
         {
           "id": "hero",
           "section": "hero",
           "priority": 1,
           "title": {
             "de": "Willkommen im Kunsthaus Zurich",
             "en": "Welcome to Kunsthaus Zurich"
           },
           "subtitle": {
             "de": "Eines der bedeutendsten Kunstmuseen der Schweiz",
             "en": "One of the most important art museums in Switzerland"
           },
           "body": {
             "de": "Entdecken Sie Meisterwerke vom 13. Jahrhundert bis zur Gegenwart in einem der grossten Kunstmuseen der Schweiz.",
             "en": "Discover masterpieces from the 13th century to the present in one of Switzerland's largest art museums."
           },
           "image": "/images/hero-kunsthaus.jpg"
         },
         {
           "id": "key-info",
           "section": "key-info",
           "priority": 2,
           "title": {
             "de": "Besucherinformationen",
             "en": "Visitor Information"
           },
           "items": [
             {
               "id": "hours",
               "title": { "de": "Offnungszeiten", "en": "Opening Hours" },
               "description": { "de": "Di/Mi/Fr/Sa/So 10-18 Uhr, Do 10-20 Uhr, Mo geschlossen", "en": "Tue/Wed/Fri/Sat/Sun 10am-6pm, Thu 10am-8pm, Mon closed" }
             },
             {
               "id": "tickets",
               "title": { "de": "Eintritt", "en": "Admission" },
               "description": { "de": "CHF 23.- / Reduziert CHF 18.- / Unter 17 gratis", "en": "CHF 23.- / Reduced CHF 18.- / Under 17 free" }
             },
             {
               "id": "location",
               "title": { "de": "Anreise", "en": "Getting Here" },
               "description": { "de": "Heimplatz 1, 8001 Zurich. Tram 3, 5, 8, 9 bis Kunsthaus", "en": "Heimplatz 1, 8001 Zurich. Tram 3, 5, 8, 9 to Kunsthaus" }
             }
           ]
         },
         {
           "id": "exhibitions",
           "section": "exhibitions",
           "priority": 3,
           "title": {
             "de": "Aktuelle Ausstellungen",
             "en": "Current Exhibitions"
           },
           "items": [
             {
               "id": "kerry-james-marshall",
               "title": { "de": "Kerry James Marshall", "en": "Kerry James Marshall" },
               "description": { "de": "Eine umfassende Retrospektive des einflussreichen amerikanischen Kunstlers", "en": "A comprehensive retrospective of the influential American artist" },
               "image": "/images/exhibitions/kerry-james-marshall.jpg",
               "date": "15.03.2026 - 22.06.2026"
             },
             {
               "id": "sammlung-highlights",
               "title": { "de": "Sammlung Highlights", "en": "Collection Highlights" },
               "description": { "de": "Meisterwerke aus der standigen Sammlung neu entdecken", "en": "Rediscover masterpieces from the permanent collection" },
               "image": "/images/exhibitions/collection-highlights.jpg"
             }
           ]
         },
         {
           "id": "offerings",
           "section": "offerings",
           "priority": 4,
           "title": {
             "de": "Unser Angebot",
             "en": "Our Offerings"
           },
           "items": [
             {
               "id": "membership",
               "title": { "de": "Mitgliedschaft", "en": "Membership" },
               "description": { "de": "Werden Sie Mitglied und geniessen Sie exklusive Vorteile", "en": "Become a member and enjoy exclusive benefits" },
               "link": "/membership"
             },
             {
               "id": "shop",
               "title": { "de": "Kunsthaus Shop", "en": "Kunsthaus Shop" },
               "description": { "de": "Kunstbucher, Poster und einzigartige Geschenke", "en": "Art books, posters and unique gifts" },
               "link": "/shop"
             }
           ]
         },
         {
           "id": "events",
           "section": "events",
           "priority": 5,
           "title": {
             "de": "Veranstaltungen & Programme",
             "en": "Events & Programs"
           },
           "body": {
             "de": "Fuhrungen, Workshops und Vortragsreihen fur alle Altersgruppen.",
             "en": "Guided tours, workshops and lecture series for all ages."
           },
           "items": [
             {
               "id": "guided-tours",
               "title": { "de": "Offentliche Fuhrungen", "en": "Public Guided Tours" },
               "description": { "de": "Jeden Samstag und Sonntag um 11 Uhr", "en": "Every Saturday and Sunday at 11am" }
             }
           ]
         }
       ]
       ```

    3. Create src/content/onsite/kerry-james-marshall.json -- On-site exhibition data per D-15, D-16, D-18, D-19:

       ```json
       [
         {
           "id": "kerry-james-marshall",
           "title": {
             "de": "Kerry James Marshall: Retrospektive",
             "en": "Kerry James Marshall: Retrospective"
           },
           "artist": "Kerry James Marshall",
           "dates": {
             "start": "2026-03-15",
             "end": "2026-06-22"
           },
           "description": {
             "de": "Kerry James Marshall (*1955, Birmingham, Alabama) ist einer der bedeutendsten zeitgenossischen Kunstler Amerikas. Seine Werke untersuchen die Geschichte und Erfahrung der afroamerikanischen Gemeinschaft durch monumentale Malerei.",
             "en": "Kerry James Marshall (*1955, Birmingham, Alabama) is one of the most important contemporary American artists. His works examine the history and experience of the African American community through monumental painting."
           },
           "artworks": [
             {
               "id": "better-homes",
               "title": { "de": "Better Homes, Better Gardens", "en": "Better Homes, Better Gardens" },
               "description": { "de": "Monumentales Gemlde, das das Leben in den Chicagoer Sozialsiedlungen darstellt.", "en": "Monumental painting depicting life in Chicago public housing projects." },
               "image": "/images/artworks/better-homes.jpg",
               "room": "Saal 2.3",
               "year": "1994",
               "medium": { "de": "Acryl und Collage auf Leinwand", "en": "Acrylic and collage on canvas" },
               "audioGuide": true,
               "membersOnly": false
             },
             {
               "id": "de-style",
               "title": { "de": "De Style", "en": "De Style" },
               "description": { "de": "Marshall verbindet afroamerikanische Kultur mit europischer Kunstgeschichte.", "en": "Marshall connects African American culture with European art history." },
               "image": "/images/artworks/de-style.jpg",
               "room": "Saal 2.3",
               "year": "1993",
               "medium": { "de": "Acryl auf Leinwand", "en": "Acrylic on canvas" },
               "audioGuide": true,
               "membersOnly": true
             },
             {
               "id": "untitled-studio",
               "title": { "de": "Untitled (Studio)", "en": "Untitled (Studio)" },
               "description": { "de": "Eine kraftvolle Darstellung des Kunstlerateliers als Ort der Selbstbehauptung.", "en": "A powerful depiction of the artist's studio as a place of self-assertion." },
               "image": "/images/artworks/untitled-studio.jpg",
               "room": "Saal 2.4",
               "year": "2014",
               "medium": { "de": "Acryl auf PVC-Platte", "en": "Acrylic on PVC panel" },
               "audioGuide": false,
               "membersOnly": false
             },
             {
               "id": "past-times",
               "title": { "de": "Past Times", "en": "Past Times" },
               "description": { "de": "Idyllische Szene, die stereotype Darstellungen von Freizeit und Zugehorigkeit hinterfragt.", "en": "Idyllic scene questioning stereotypical representations of leisure and belonging." },
               "image": "/images/artworks/past-times.jpg",
               "room": "Saal 2.5",
               "year": "1997",
               "medium": { "de": "Acryl auf Leinwand", "en": "Acrylic on canvas" },
               "audioGuide": true,
               "membersOnly": true
             }
           ],
           "practicalInfo": {
             "rooms": ["Saal 2.3", "Saal 2.4", "Saal 2.5"],
             "audioGuideAvailable": true,
             "estimatedDuration": {
               "de": "Ca. 60-90 Minuten",
               "en": "Approx. 60-90 minutes"
             }
           }
         }
       ]
       ```

    4. Create src/content/shared/navigation.json:

       ```json
       [
         { "id": "home", "label": { "de": "Startseite", "en": "Home" }, "href": "/" },
         { "id": "exhibitions", "label": { "de": "Ausstellungen", "en": "Exhibitions" }, "href": "/exhibitions" },
         { "id": "visit", "label": { "de": "Besuch planen", "en": "Plan Your Visit" }, "href": "/visit" },
         { "id": "collection", "label": { "de": "Sammlung", "en": "Collection" }, "href": "/collection" },
         { "id": "about", "label": { "de": "Uber uns", "en": "About" }, "href": "/about" },
         { "id": "mode-toggle", "label": { "de": "Modus wechseln", "en": "Switch Mode" }, "icon": "toggle" }
       ]
       ```
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro check 2>&1 | tail -10 && node -e "const d=require('./src/content/planning/homepage.json'); console.log('Planning sections:', d.length); const o=require('./src/content/onsite/kerry-james-marshall.json'); console.log('Onsite entries:', o.length, 'artworks:', o[0].artworks.length)" 2>/dev/null || echo "check via astro build" && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/content.config.ts contains `defineCollection` (3 times)
    - src/content.config.ts contains `file("src/content/planning/homepage.json")`
    - src/content.config.ts contains `file("src/content/onsite/kerry-james-marshall.json")`
    - src/content.config.ts contains `file("src/content/shared/navigation.json")`
    - src/content.config.ts contains `localizedText` schema with `de: z.string()`, `en: z.string()`, `fr: z.string().optional()`
    - src/content.config.ts contains `export const collections`
    - src/content/planning/homepage.json is valid JSON with at least 5 entries
    - src/content/planning/homepage.json first entry has `"id": "hero"` and `"section": "hero"` and `"priority": 1`
    - src/content/planning/homepage.json contains entries with `"de":` and `"en":` in title objects
    - src/content/onsite/kerry-james-marshall.json contains `"artist": "Kerry James Marshall"`
    - src/content/onsite/kerry-james-marshall.json contains at least 3 artworks with `"room":` fields
    - src/content/onsite/kerry-james-marshall.json contains `"audioGuide": true` and `"membersOnly": true` on some artworks
    - src/content/shared/navigation.json is valid JSON with at least 5 entries
    - `npx astro build` succeeds (schema validation passes)
  </acceptance_criteria>
  <done>Three content collections defined with Zod schemas. Planning homepage has 5+ sections following priority hierarchy. On-site has Kerry James Marshall exhibition with 4+ artworks. Shared has navigation entries. Build passes schema validation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire content collections into index page to prove data flow</name>
  <files>
    src/pages/index.astro
  </files>
  <read_first>
    src/content.config.ts (schema definitions from Task 1)
    src/content/planning/homepage.json (data structure to render)
    src/pages/index.astro (current state from Plan 01 or Plan 02)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 usage example)
  </read_first>
  <action>
    1. Update src/pages/index.astro to load and render content from collections:

       ```astro
       ---
       import BaseLayout from '../layouts/BaseLayout.astro';
       import { getCollection } from 'astro:content';

       const planningContent = await getCollection('planning');
       const sorted = planningContent.sort((a, b) => a.data.priority - b.data.priority);

       const lang = 'de'; // Default language, Phase 2 will add switching
       ---
       <BaseLayout title="Kunsthaus Zurich">
         <div class="grid-page">
           {sorted.map((section) => (
             <section class="col-full" style={`padding: var(--space-12) var(--space-8);`}>
               <h2 style="font-size: var(--type-2xl); margin-bottom: var(--space-4);">
                 {section.data.title[lang]}
               </h2>
               {section.data.subtitle && (
                 <p style="font-size: var(--type-lg); color: var(--color-accent); margin-bottom: var(--space-4);">
                   {section.data.subtitle[lang]}
                 </p>
               )}
               {section.data.body && (
                 <p style="font-size: var(--type-md); max-width: 60ch; line-height: var(--leading-relaxed);">
                   {section.data.body[lang]}
                 </p>
               )}
               {section.data.items && (
                 <ul style="list-style: none; margin-top: var(--space-6); display: flex; flex-direction: column; gap: var(--space-4);">
                   {section.data.items.map((item) => (
                     <li style="padding: var(--space-4); background: var(--color-decorative); border-radius: 2px;">
                       <strong>{item.title[lang]}</strong>
                       {item.description && <p style="margin-top: var(--space-2);">{item.description[lang]}</p>}
                       {item.date && <span style="font-size: var(--type-sm); color: var(--color-decorative-shadow);">{item.date}</span>}
                     </li>
                   ))}
                 </ul>
               )}
             </section>
           ))}
           <!-- Content source indicator -->
           <footer class="col-full" style="padding: var(--space-8); font-size: var(--type-sm); color: var(--color-decorative-shadow);">
             Content loaded from JSON content collections. {planningContent.length} sections rendered.
           </footer>
         </div>
       </BaseLayout>
       ```

       NOTE: This is a foundation proof-of-concept rendering. Phase 2 replaces this with proper component-based sections (Hero, Exhibitions, VisitInfo, etc.). The goal here is to prove the JSON -> template data flow works end-to-end.
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro build 2>&1 | tail -5 && grep "getCollection" src/pages/index.astro && grep "section.data.title" src/pages/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `import { getCollection } from 'astro:content'`
    - src/pages/index.astro contains `getCollection('planning')`
    - src/pages/index.astro contains `section.data.title[lang]`
    - src/pages/index.astro contains `section.data.items` with map rendering
    - src/pages/index.astro does NOT contain hardcoded content strings like "Willkommen" (content comes from JSON)
    - `npx astro build` succeeds without errors
    - Built output in dist/index.html contains rendered content from homepage.json (e.g., "Besucherinformationen")
  </acceptance_criteria>
  <done>Index page renders all Planning mode content sections from JSON. Content is sorted by priority. No hardcoded text in templates. Build output contains rendered content proving the full data flow: JSON -> Content Collection -> Zod validation -> Astro template -> HTML.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npx astro build` succeeds -- schema validation catches any malformed JSON
2. Built dist/index.html contains text from homepage.json (search for "Besucherinformationen" or "Visitor Information")
3. Content collections are queryable: `getCollection('planning')`, `getCollection('onsite')`, `getCollection('shared')`
4. JSON files have i18n structure (de, en keys on all text fields)
5. Planning content has priority ordering matching D-13 hierarchy
6. On-site content has artwork entries with room and audioGuide fields
</verification>

<success_criteria>
- Three content collections defined with Zod schemas (planning, onsite, shared)
- JSON content files populated with realistic mock data
- Planning mode follows D-13 priority hierarchy (Inspiration -> Key Info -> Exhibitions -> ...)
- On-site mode features Kerry James Marshall with artworks, rooms, audio guide flags
- Index page renders content from JSON -- zero hardcoded text
- Build succeeds with schema validation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
