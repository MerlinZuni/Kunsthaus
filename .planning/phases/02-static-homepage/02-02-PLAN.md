---
phase: 02-static-homepage
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/nav/NavBar.astro
  - src/components/nav/LogoIsland.astro
  - src/components/nav/SearchOverlay.astro
  - src/components/nav/HamburgerOverlay.astro
  - src/components/nav/LanguageSwitcher.astro
  - src/components/hero/HeroCarousel.astro
  - src/components/hero/HeroSlide.astro
  - src/components/hero/CarouselTabs.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - NAV-01
  - HOME-01

must_haves:
  truths:
    - "Navigation bar displays search icon, centered logo, 4 nav links, and language switcher"
    - "Mobile shows search icon, centered logo, hamburger icon -- never hidden on scroll"
    - "Hamburger opens full-screen overlay with large nav links, language options, search field"
    - "Hero displays monumental KUNST / HAUS / ZURICH right-aligned three-line typography"
    - "Hero carousel cycles through featured exhibitions with progress tabs"
    - "Carousel has a visible pause/play toggle for WCAG 2.2.2 compliance"
    - "Search icon opens visual shell overlay with input and suggestion links"
    - "Navigation uses content from JSON, not hardcoded text"
  artifacts:
    - path: "src/components/nav/NavBar.astro"
      provides: "Responsive floating navigation bar"
      contains: "nav"
    - path: "src/components/nav/HamburgerOverlay.astro"
      provides: "Full-screen mobile menu"
      contains: "hamburger"
    - path: "src/components/hero/HeroCarousel.astro"
      provides: "Hero section with carousel and scattered images"
      contains: "aria-roledescription"
    - path: "src/components/hero/CarouselTabs.astro"
      provides: "Progress tabs for carousel auto-play"
      contains: "tab-fill"
  key_links:
    - from: "src/components/nav/NavBar.astro"
      to: "src/content/shared/navigation.json"
      via: "getCollection('shared')"
      pattern: "getCollection"
    - from: "src/components/hero/HeroCarousel.astro"
      to: "src/content/planning/homepage.json"
      via: "getCollection('planning') hero slides"
      pattern: "getCollection"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/nav/NavBar.astro"
      via: "Astro component import"
      pattern: "import NavBar"
---

<objective>
Build the navigation bar (NAV-01) and hero carousel (HOME-01) -- the entire top-of-page experience. Navigation includes floating bar with velocity-aware scroll behavior, logo island persistence, search overlay shell, hamburger full-screen overlay, and language switcher. Hero includes the monumental "KUNST / HAUS / ZURICH" typography, scattered exhibition images, auto-play carousel with labeled progress tabs, and pause/play toggle.

Purpose: These are the first things visitors see. The nav establishes interaction patterns used everywhere. The hero creates the monumental first impression that wins the competition.

Output: Nav and hero components integrated into BaseLayout, rendering real JSON content.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-homepage/02-CONTEXT.md
@.planning/phases/02-static-homepage/02-RESEARCH.md
@.planning/phases/02-static-homepage/02-UI-SPEC.md
@.planning/phases/02-static-homepage/02-01-SUMMARY.md

@src/layouts/BaseLayout.astro
@src/styles/components/interactions.css
@src/styles/components/sections.css
@src/content.config.ts
@src/content/shared/navigation.json
@src/content/shared/ui-strings.json
@src/content/planning/homepage.json

<interfaces>
<!-- From Plan 01: content collections available -->
getCollection('shared')     -> navigation items: { id, label: {de, en}, href }
getCollection('uiStrings')  -> { id, key, value: {de, en, fr?} }
getCollection('planning')   -> homepage sections including hero with slides array

<!-- From Plan 01: CSS classes available -->
.hover-wipe         -> clip-path background wipe on hover (200ms/350ms asymmetric)
.hover-wipe-inverse -> inverse wipe for filled CTAs
.touch-pulse        -> scale(0.96) on :active
.touch-target       -> min 44x44px
.hero-pinned        -> position: fixed, top:0, width:100%, height:100vh, z-index:10
.hero-spacer        -> height:100vh pushes content below fixed hero

<!-- Z-index scale from UI-SPEC -->
Hero: 10, Sections: 20-27, CTA: 50, Nav: 60, Logo Island: 65, Overlays: 80, Skip nav: 100

<!-- Typography from typography.css -->
--type-sm: 0.75rem, --type-md: 1rem, --type-lg: 1.333rem
--type-2xl: 2.369rem, --type-3xl: 3.157rem, --type-4xl: 4.209rem
--weight-regular: 400, --weight-bold: 700
--leading-tight: 1.1, --leading-normal: 1.4

<!-- Spacing from primitives.css -->
--space-2: 0.5rem, --space-4: 1rem, --space-6: 1.5rem, --space-8: 2rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation bar with scroll behavior, overlays, and language switcher</name>
  <files>
    src/components/nav/NavBar.astro,
    src/components/nav/LogoIsland.astro,
    src/components/nav/SearchOverlay.astro,
    src/components/nav/HamburgerOverlay.astro,
    src/components/nav/LanguageSwitcher.astro,
    src/layouts/BaseLayout.astro
  </files>
  <read_first>
    src/layouts/BaseLayout.astro,
    src/content/shared/navigation.json,
    src/content/shared/ui-strings.json,
    src/content.config.ts,
    src/styles/components/interactions.css,
    .planning/phases/02-static-homepage/02-CONTEXT.md,
    .planning/phases/02-static-homepage/02-UI-SPEC.md
  </read_first>
  <action>
**Create `src/components/nav/NavBar.astro`** per D-07, D-08, D-10:
- Accepts `navItems` prop (from getCollection('shared')) and `lang` prop ('en' | 'de')
- Renders `<header>` with `position: fixed`, `z-index: 60`, full-width
- Background: `#EBEBEB` (var(--color-concrete-light) from primitives) with transparent padding around the bar (use padding on outer wrapper, background on inner bar)
- Layout: flexbox row with `justify-content: space-between; align-items: center`
- **Left:** Search icon button -- inline SVG magnifying glass (24x24 viewBox, 2px stroke, `currentColor`), `.touch-target` class, `aria-label` from ui-strings "search-label", opens SearchOverlay on click
- **Center:** Kunsthaus Zurich logo text -- `<a href="/">` with text "Kunsthaus Zurich" (or SVG wordmark if preferred), styled with `mix-blend-mode: exclusion` per D-08
- **Right:** flex row of nav links from `navItems` prop (each `<a>` with `.hover-wipe .touch-pulse` classes, font: `var(--type-md)` / `var(--weight-regular)`) + LanguageSwitcher component
- All text comes from props/JSON, no hardcoded labels

**Desktop scroll behavior (D-10)** -- in a `<script>` tag:
- Import gsap and ScrollTrigger
- Create ScrollTrigger monitoring scroll position and velocity via `self.getVelocity()`
- Start after 100px scroll (`start: 'top -100'`)
- Fast scroll (velocity > 1500): instantly hide nav (`opacity: 0, duration: 0.15`), show LogoIsland (`opacity: 1, duration: 0.3`)
- Slow scroll up or micro-pause (velocity < 300 && direction === -1): reveal nav with blur-expand (`opacity: 1, filter: blur(0px), duration: 0.4`), hide LogoIsland
- Search icon migrates to a fixed corner position when nav dissolves (add `.search-migrated` class, position with CSS)

**Mobile (D-12)** -- `@media (max-width: 767px)`:
- Simplified bar: search icon | logo (centered) | hamburger icon (3-line SVG, 24x24, `.touch-target`)
- Never hidden on scroll (no velocity behavior on mobile)
- Hamburger button toggles HamburgerOverlay visibility

**Create `src/components/nav/LogoIsland.astro`** per D-10:
- `position: fixed`, `top: var(--space-4)`, centered horizontally, `z-index: 65`
- Contains logo text/SVG with `mix-blend-mode: exclusion`
- Initially hidden (`opacity: 0`), shown by NavBar scroll script
- Desktop only -- hidden on mobile via media query

**Create `src/components/nav/SearchOverlay.astro`** per D-11:
- Visual shell only, no functional search (deferred to 999.1)
- `position: fixed`, full viewport, `z-index: 80`, background: `var(--color-surface)` with 95% opacity
- Contains: text input field (styled, placeholder "Search Kunsthaus..."), 6 hardcoded suggestion links below (e.g., "Current Exhibitions", "Opening Hours", "Tickets", "Kerry James Marshall", "Membership", "Getting Here") -- links point to `#` placeholders
- Close button (X icon, top-right, `.touch-target`)
- Initially hidden (`display: none`), toggled by search icon click
- When open: `document.body.style.overflow = 'hidden'` and call `lenis.stop()` if available
- When closed: reverse both

**Create `src/components/nav/HamburgerOverlay.astro`** per D-13:
- Full-screen overlay, `position: fixed`, full viewport, `z-index: 80`
- Background: `var(--color-surface)`
- Kunsthaus Zurich logo centered at top
- Nav links stacked vertically -- large type (`var(--type-2xl)`, `var(--weight-bold)`) with `.hover-wipe` class
- Language options (EN / DE / FR) displayed as horizontal buttons below nav links
- Search field at bottom (text input, same style as SearchOverlay)
- Close button (X icon) or tapping hamburger again closes
- Body scroll lock: same pattern as SearchOverlay (`overflow: hidden` + `lenis.stop()`)
- Initially hidden, toggled by hamburger button

**Create `src/components/nav/LanguageSwitcher.astro`**:
- Button showing current language code ("EN"), clicking opens dropdown
- Dropdown: 3 options (EN, DE, FR) -- clicking sets `lang` attribute on `<html>` and stores in localStorage
- Styled: `var(--type-md)`, dropdown positioned absolutely below button
- Close on click outside

**Update `src/layouts/BaseLayout.astro`**:
- Import NavBar and LogoIsland
- Add NavBar above `<main>` (still inside `<body>`), pass `navItems` and `lang` as props
- Add LogoIsland after NavBar
- Fetch navigation data and ui-strings via getCollection in the frontmatter script
- Add `lang` prop to BaseLayout interface (default 'en')
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav/NavBar.astro contains `<header` and `position: fixed` and `z-index: 60`
    - src/components/nav/NavBar.astro contains `mix-blend-mode: exclusion`
    - src/components/nav/NavBar.astro contains `getVelocity` (velocity-aware scroll behavior)
    - src/components/nav/NavBar.astro contains `@media (hover: hover)` or uses `.hover-wipe` class on links
    - src/components/nav/NavBar.astro contains `aria-label` on search button
    - src/components/nav/LogoIsland.astro contains `z-index: 65` and `mix-blend-mode: exclusion`
    - src/components/nav/SearchOverlay.astro contains `z-index: 80` and an `<input` element
    - src/components/nav/SearchOverlay.astro contains `lenis` (scroll lock reference)
    - src/components/nav/HamburgerOverlay.astro contains `z-index: 80` and nav link elements
    - src/components/nav/HamburgerOverlay.astro contains `overflow` (body scroll lock)
    - src/components/nav/LanguageSwitcher.astro contains "EN" and "DE" and "FR"
    - src/layouts/BaseLayout.astro contains `import NavBar` and `import LogoIsland`
    - src/layouts/BaseLayout.astro contains `getCollection`
    - `npx astro build` passes
  </acceptance_criteria>
  <done>Navigation bar renders with search icon, centered logo with exclusion blend, 4 nav links from JSON, and language switcher. Velocity-aware scroll hides nav and shows logo island on desktop. Mobile shows simplified bar that never hides. Search overlay shell and hamburger full-screen overlay open/close with body scroll lock.</done>
</task>

<task type="auto">
  <name>Task 2: Hero carousel with monumental typography, scattered images, and progress tabs</name>
  <files>
    src/components/hero/HeroCarousel.astro,
    src/components/hero/HeroSlide.astro,
    src/components/hero/CarouselTabs.astro
  </files>
  <read_first>
    src/content/planning/homepage.json,
    src/content/shared/ui-strings.json,
    src/content.config.ts,
    src/styles/components/sections.css,
    src/styles/components/interactions.css,
    .planning/phases/02-static-homepage/02-CONTEXT.md,
    .planning/phases/02-static-homepage/02-UI-SPEC.md
  </read_first>
  <action>
**Create `src/components/hero/HeroCarousel.astro`** per D-01 through D-06, D-41:
- Accepts `slides` prop (array from homepage.json hero slides), `lang` prop, and `uiStrings` prop
- Outer wrapper: `<section aria-roledescription="carousel" aria-label="Featured exhibitions">` with class `hero-pinned` (from sections.css -- position:fixed, z-index:10)
- Inner container uses the 48-track grid (class `grid-page`) for layout

**Typography (D-01):**
- "KUNST", "HAUS", "ZURICH" as three separate block elements, right-aligned
- Font: `var(--type-4xl)` with clamp: `font-size: clamp(var(--type-3xl), 8vw, var(--type-4xl))`
- Weight: `var(--weight-bold)`, line-height: `var(--leading-tight)`, text-transform: uppercase
- Color: `var(--color-text)`
- Position: grid-aligned to right side (e.g., grid-column: 25 / -1 on desktop), topmost z-index within hero
- Each word on its own line, right-aligned (`text-align: right`)

**Slide rendering:**
- Each slide rendered as `<div role="group" aria-roledescription="slide" aria-label="{n} of {total}: {slide title}">`
- Only active slide visible (others `display: none` or `opacity: 0`)
- Each slide contains: HeroSlide component with scattered images + subtitle text + CTA button

**Auto-play (D-03):**
- `<script>` tag with GSAP timeline:
  - `gsap.timeline({ repeat: -1 })` with SLIDE_DURATION = 5 seconds per slide
  - Each slide: activate slide (show), fill progress bar for that tab, then transition to next
  - Use `onStart` callback to toggle `.active` class on slides and tabs
  - `aria-live="polite"` container updated with current slide info on each transition

**Carousel tabs (D-04):** Render CarouselTabs component below the hero content area

**Pause/play toggle (D-05, WCAG 2.2.2):**
- Button with `aria-label` from ui-strings (carousel-pause / carousel-play)
- Toggles `tl.pause()` / `tl.resume()`
- Auto-pause when `prefers-reduced-motion: reduce` is active
- Auto-pause when keyboard focus enters the carousel

**Create `src/components/hero/HeroSlide.astro`** per D-02:
- Accepts `images` array (each with src, alt, gridCol, gridRow) and `subtitle`, `cta` props
- Images positioned in offset scattered arrangement using CSS grid:
  - Each image placed at its `gridCol` / `gridRow` position
  - Images overlap (z-index stacking, slight offsets with CSS transforms)
  - `object-fit: cover` on all images
  - Images layer UNDERNEATH the typography (lower z-index)
- Subtitle text: `var(--type-lg)`, positioned below or beside images
- CTA button: `.hover-wipe-inverse .touch-pulse` classes, links to `cta.href` (placeholder), text from `cta.label[lang]`
- All image `src` paths point to `/images/exhibitions/` -- these files won't exist yet (placeholder), use `alt` text as fallback. Add `loading="eager"` since hero is above fold.

**Create `src/components/hero/CarouselTabs.astro`** per D-04:
- Accepts `slides` array and `lang` prop
- Renders one tab per slide: exhibition title text + animated fill bar
- Each tab: `<button data-tab="{index}">` with:
  - Title text: `var(--type-sm)`, showing exhibition title from `slide.title[lang]`
  - Fill bar: `<span class="tab-fill">` -- a horizontal bar that scales from 0 to 1 (scaleX) during auto-play via GSAP
  - Fill bar CSS: `transform-origin: left`, `background: var(--color-text)`, height 2px, initially `scaleX(0)`
- Active tab: `.active` class adds visual indicator (bolder text or highlighted)
- Clicking a tab jumps carousel to that slide (dispatches custom event or directly seeks GSAP timeline)
- Mobile (D-04): active title visible, inactive tabs collapse to compact dot indicators. Use `@media (max-width: 767px)` to hide tab text for inactive tabs and show small dots instead.

**Hero spacer:**
The page needs a `<div class="hero-spacer">` after the hero component in the page (not inside HeroCarousel) to push subsequent stacking sections below the fixed hero. This will be placed in index.astro (Plan 04), but document this requirement.
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/hero/HeroCarousel.astro contains `aria-roledescription="carousel"`
    - src/components/hero/HeroCarousel.astro contains `aria-live="polite"`
    - src/components/hero/HeroCarousel.astro contains "KUNST" and "HAUS" and "ZURICH" (or renders from data -- either way the words must appear)
    - src/components/hero/HeroCarousel.astro contains `clamp(` for responsive hero typography
    - src/components/hero/HeroCarousel.astro contains `gsap.timeline` (auto-play)
    - src/components/hero/HeroCarousel.astro contains `prefers-reduced-motion` (respects motion preference)
    - src/components/hero/HeroCarousel.astro contains `hero-pinned` class reference
    - src/components/hero/HeroSlide.astro contains `object-fit: cover` or `object-fit:cover`
    - src/components/hero/HeroSlide.astro contains `hover-wipe-inverse` (CTA button)
    - src/components/hero/CarouselTabs.astro contains `tab-fill` class
    - src/components/hero/CarouselTabs.astro contains `scaleX` (fill bar animation)
    - src/components/hero/CarouselTabs.astro contains `data-tab` attribute
    - `npx astro build` passes
  </acceptance_criteria>
  <done>Hero section renders monumental right-aligned three-line KUNST/HAUS/ZURICH typography. Carousel cycles through exhibition slides with scattered image layout. Progress tabs show exhibition titles with animated fill bars. Pause/play toggle present and functional. Carousel pauses on reduced-motion. ARIA carousel pattern implemented.</done>
</task>

</tasks>

<verification>
- Navigation bar visible at top with search, logo, 4 links, language switcher
- Mobile: simplified bar with hamburger, overlays open/close
- Hero: KUNST/HAUS/ZURICH in large bold right-aligned type
- Carousel auto-plays through slides with progress tab fill animation
- Pause button stops carousel
- `npx astro build` passes
</verification>

<success_criteria>
- NAV-01 complete: responsive nav with mobile hamburger, search overlay, language switcher, velocity-aware scroll behavior
- HOME-01 complete: hero carousel with monumental typography, scattered images, progress tabs, pause/play, ARIA pattern
- All content from JSON, no hardcoded text
- Interaction states (hover wipe, touch pulse) applied to nav links and hero CTA
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-homepage/02-02-SUMMARY.md`
</output>
