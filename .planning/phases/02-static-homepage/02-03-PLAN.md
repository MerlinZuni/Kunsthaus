---
phase: 02-static-homepage
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/SectionWrapper.astro
  - src/components/sections/CinematicReveal.astro
  - src/components/sections/PinnedNarrative.astro
  - src/components/sections/ExhibitionCard.astro
  - src/components/sections/StandardGrid.astro
  - src/components/ui/StickyCTA.astro
  - src/components/ui/ModeToggle.tsx
  - src/components/ui/SectionProgress.tsx
  - src/components/ui/ReduceMotionToggle.astro
autonomous: true
requirements:
  - HOME-02
  - HOME-03

must_haves:
  truths:
    - "Exhibition section shows Cinematic Reveal for featured exhibition and Pinned Narrative for exhibition list"
    - "Exhibition cards display 60/40 image-to-text split with 3:2 aspect ratio images"
    - "Visit info section renders hours, location, tickets from JSON content"
    - "Offerings and Education sections use Pinned Narrative pattern with cards"
    - "Section stacking effect works on desktop -- sections pin and scale down as next section scrolls over"
    - "Mode toggle switches data-mode attribute on html element between planning and onsite"
    - "Section progress indicator shows current section fraction and progress arc"
    - "Sticky CTA visible: floating buttons on desktop, bottom bar on mobile"
  artifacts:
    - path: "src/components/sections/SectionWrapper.astro"
      provides: "Section container with z-index, bg rotation, stacking GSAP"
      contains: "stacking-section"
    - path: "src/components/sections/CinematicReveal.astro"
      provides: "Full-bleed image reveal strip with clipPath"
      contains: "clipPath"
    - path: "src/components/sections/PinnedNarrative.astro"
      provides: "Pinned scrolling card sequence"
      contains: "ScrollTrigger"
    - path: "src/components/ui/ModeToggle.tsx"
      provides: "React island for planning/onsite mode switching"
      contains: "data-mode"
    - path: "src/components/ui/SectionProgress.tsx"
      provides: "React island for section fraction counter + progress arc"
      contains: "aria-live"
  key_links:
    - from: "src/components/sections/SectionWrapper.astro"
      to: "src/styles/components/sections.css"
      via: "CSS classes"
      pattern: "stacking-section"
    - from: "src/components/ui/ModeToggle.tsx"
      to: "document.documentElement"
      via: "setAttribute('data-mode')"
      pattern: "data-mode"
    - from: "src/components/sections/PinnedNarrative.astro"
      to: "src/components/sections/ExhibitionCard.astro"
      via: "Astro component import"
      pattern: "import ExhibitionCard"
---

<objective>
Build all homepage content sections (exhibitions, visit info, offerings, education, key-info) using the two immersive storytelling components (Cinematic Reveal Strip, Pinned Narrative Sequence), plus the sticky CTA with mode toggle, section progress indicator, and reduce-motion toggle. This is the body of the page between hero and footer.

Purpose: These sections contain the actual museum content that visitors engage with. The immersive storytelling components (Cinematic Reveal, Pinned Narrative) create the scroll-driven experience that differentiates this prototype. The mode toggle is the core dual-mode interaction.

Output: All section components, sticky CTA, section progress, and immersive storytelling patterns ready for page composition.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-homepage/02-CONTEXT.md
@.planning/phases/02-static-homepage/02-RESEARCH.md
@.planning/phases/02-static-homepage/02-UI-SPEC.md
@.planning/phases/02-static-homepage/02-01-SUMMARY.md

@src/content.config.ts
@src/content/planning/homepage.json
@src/content/shared/ui-strings.json
@src/styles/components/sections.css
@src/styles/components/interactions.css
@src/styles/grid.css
@src/styles/tokens/primitives.css
@src/styles/tokens/semantic.css

<interfaces>
<!-- From Plan 01: CSS classes -->
.stacking-section           -> section wrapper with will-change, min-height:100vh
.stacking-section[data-bg-step="1-4"] -> background color + texture rotation
.section-spacing             -> vertical padding: 128px desktop, 96px tablet, 64px mobile
.hero-pinned / .hero-spacer  -> hero fixed positioning
.hover-wipe / .hover-wipe-inverse / .touch-pulse / .touch-target / .link-underline

<!-- From content.config.ts: homepage JSON structure -->
planningHomepage schema: { id, section, priority, title: localizedText, subtitle?, body?, image?, items?[], slides?[] }
items: { id, title: localizedText, description?, image?, date?, link? }
uiStrings: { id, key, value: localizedText }

<!-- Grid classes from grid.css -->
.grid-page -> 48-track grid container
.col-1 through .col-12 -> span helpers (4 tracks each)
.col-start-1 through .col-start-12 -> position helpers
.col-full -> span all tracks

<!-- Z-index scale -->
Sections: 20-27 (incrementing), CTA: 50, Section Progress: 50
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section wrapper, Cinematic Reveal Strip, and Pinned Narrative Sequence</name>
  <files>
    src/components/sections/SectionWrapper.astro,
    src/components/sections/CinematicReveal.astro,
    src/components/sections/PinnedNarrative.astro,
    src/components/sections/ExhibitionCard.astro,
    src/components/sections/StandardGrid.astro
  </files>
  <read_first>
    src/styles/components/sections.css,
    src/styles/grid.css,
    src/styles/components/interactions.css,
    src/content/planning/homepage.json,
    .planning/phases/02-static-homepage/02-CONTEXT.md,
    .planning/phases/02-static-homepage/02-RESEARCH.md
  </read_first>
  <action>
**Create `src/components/sections/SectionWrapper.astro`** per D-25, D-26, D-27:
- Props: `index: number` (section order, 0-based), `bgStep: 1|2|3|4`, `sectionName: string`, `sectionId: string`
- Renders `<section>` with:
  - `class="stacking-section section-spacing"` (from sections.css)
  - `data-bg-step={bgStep}` (background rotation from sections.css)
  - `data-section-index={index}` (for section progress tracking)
  - `data-section-name={sectionName}` (for a11y announcements)
  - `id={sectionId}` (for skip-nav and deep links)
  - `style={`z-index: ${20 + index}`}` (incrementing z-index per UI-SPEC)
- Contains `<slot />` for section content
- `<script>` block with GSAP ScrollTrigger stacking effect using `ScrollTrigger.matchMedia()`:
  - Desktop (min-width: 1024px): `pin: true`, `pinSpacing: false`. `onUpdate`: `scale = 1 - (progress * 0.1)`, `rotation = progress * (i % 2 === 0 ? 2 : -3)`, `opacity = 1 - (progress * 0.6)`. Last section does NOT pin.
  - Tablet (768-1023px): same but no rotation (scale + opacity only)
  - Mobile (<768px): no pinning. Simple `gsap.from` with `opacity: 0, y: 40, scrollTrigger: { scrub: true, start: 'top 80%', end: 'top 40%' }`
  - Reduced motion: kill all ScrollTriggers, static layout
- Call `ScrollTrigger.refresh()` after all pins are created

**Create `src/components/sections/CinematicReveal.astro`** per D-28:
- Props: `contentType: string`, `data: { title: localizedText, subtitle?: localizedText, image?: string, description?: localizedText, cta?: { label: localizedText, href: string } }`, `lang: string`
- Renders full-bleed container using `.col-full` on `.grid-page`
- Image container: full-width, `aspect-ratio: 16/9`, `overflow: hidden`
  - Image: `object-fit: cover`, `width: 100%`, `height: 100%`
  - Colored overlay `<div>` on top: `background: var(--color-surface)`, `position: absolute`, `inset: 0`
- Title: `var(--type-2xl)`, `var(--weight-bold)`, positioned to enter from left side
- Editorial line / subtitle: `var(--type-md)`, positioned to enter from right side
- CTA button if provided: `.hover-wipe-inverse .touch-pulse`
- `<script>` with GSAP ScrollTrigger scroll-driven reveal:
  - Overlay fades out (opacity 1 -> 0) as scroll progresses
  - Image mask expands via `clipPath: inset(20% 20% 20% 20%)` -> `inset(0% 0% 0% 0%)`
  - Title enters from left (`x: -60` -> `x: 0`), editorial from right (`x: 60` -> `x: 0`)
  - `scrub: true`, `start: 'top 80%'`, `end: 'top 20%'`
- Mobile: vertical wipe direction (clipPath from bottom instead of all sides), text below image (flexbox column)

**Create `src/components/sections/PinnedNarrative.astro`** per D-28, D-33:
- Props: `contentType: string`, `items: array`, `lang: string`
- Container: full width, uses `.grid-page` internally for card positioning
- Each item rendered as ExhibitionCard component (or generic card for non-exhibition content)
- `<script>` with GSAP ScrollTrigger:
  - Desktop: section pins (`pin: true`), cards scroll through one at a time
  - Cards offset alternating left (grid-column 1/7) and right (grid-column 7/13)
  - Card transitions: current card fades out + slides slightly in one direction, next card fades in from opposite (crossfade with offset slide per D-33)
  - Background color shifts between cards (alternate between bg-step 1 and 2)
  - Total scroll distance: `items.length * 100vh` (each card gets 100vh of scroll)
  - Mobile (matchMedia <768px): unpin, cards stack vertically as normal scroll with gap `var(--space-16)`

**Create `src/components/sections/ExhibitionCard.astro`** per D-31, D-32:
- Props: `title: localizedText`, `description?: localizedText`, `image?: string`, `date?: string`, `cta?: { label: localizedText, href: string }`, `lang: string`
- Layout: vertical split -- image area (60vh) + text area (40vh) on desktop, 50/50 on mobile
- Image: `aspect-ratio: 3/2`, `object-fit: cover`, full width of card area. If no image src exists, show placeholder div with `background: var(--color-decorative)` and alt text centered
- Text area: `padding: var(--space-6)`
  - Title: `var(--type-lg)`, `var(--weight-bold)`
  - Date: `var(--type-sm)`, `color: var(--color-decorative-shadow)` (if provided)
  - Description: `var(--type-md)`, `var(--leading-normal)`, `max-width: 45ch`
  - CTA: `.hover-wipe-inverse .touch-pulse`, text from `cta.label[lang]`
- `.touch-pulse` class on entire card for touch interaction

**Create `src/components/sections/StandardGrid.astro`** per D-29 for key-info, events, planning-details:
- Props: `title: localizedText`, `body?: localizedText`, `items: array`, `lang: string`, `columns: 2|3|4` (default 3)
- Renders section heading: `var(--type-2xl)`, `var(--weight-bold)`, `margin-bottom: var(--space-8)`
- Optional body text: `var(--type-md)`, `max-width: 60ch`, `margin-bottom: var(--space-8)`
- Items in CSS grid: `grid-template-columns: repeat({columns}, 1fr)` on desktop, 2 cols on tablet, 1 col on mobile
- Each item: title (`var(--type-lg)`, `var(--weight-bold)`), description (`var(--type-md)`), optional date/link
- Items with links: `.hover-wipe .touch-pulse` on the link
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/SectionWrapper.astro contains `stacking-section` and `data-bg-step` and `data-section-index`
    - src/components/sections/SectionWrapper.astro contains `ScrollTrigger.matchMedia` (responsive stacking)
    - src/components/sections/SectionWrapper.astro contains `pinSpacing: false`
    - src/components/sections/SectionWrapper.astro contains `prefers-reduced-motion`
    - src/components/sections/CinematicReveal.astro contains `clipPath` or `clip-path` (mask expansion)
    - src/components/sections/CinematicReveal.astro contains `scrub` (scroll-driven)
    - src/components/sections/PinnedNarrative.astro contains `pin: true` (pinned on desktop)
    - src/components/sections/PinnedNarrative.astro contains `matchMedia` (responsive unpin on mobile)
    - src/components/sections/ExhibitionCard.astro contains `aspect-ratio` and `object-fit: cover`
    - src/components/sections/ExhibitionCard.astro contains `60vh` or equivalent for image area
    - src/components/sections/StandardGrid.astro contains `grid-template-columns`
    - `npx astro build` passes
  </acceptance_criteria>
  <done>SectionWrapper renders stacking sections with GSAP ScrollTrigger (pin, scale, rotate on desktop; scale+opacity on tablet; fade-up on mobile; nothing on reduced-motion). CinematicReveal does scroll-driven image reveal with clipPath expansion. PinnedNarrative pins and scrolls through cards. ExhibitionCard shows 60/40 image-text split. StandardGrid handles informational sections.</done>
</task>

<task type="auto">
  <name>Task 2: Sticky CTA, mode toggle, section progress indicator, and reduce-motion toggle</name>
  <files>
    src/components/ui/StickyCTA.astro,
    src/components/ui/ModeToggle.tsx,
    src/components/ui/SectionProgress.tsx,
    src/components/ui/ReduceMotionToggle.astro
  </files>
  <read_first>
    src/content/shared/ui-strings.json,
    src/content.config.ts,
    src/styles/components/interactions.css,
    src/components/GridOverlay.tsx,
    .planning/phases/02-static-homepage/02-CONTEXT.md,
    .planning/phases/02-static-homepage/02-UI-SPEC.md
  </read_first>
  <action>
**Create `src/components/ui/ModeToggle.tsx`** per D-18 through D-22 (React island):
- Props: `labels: { planning: { en, de, fr }, onsite: { en, de, fr } }`, `lang: 'en' | 'de'`
- State: `mode` ('planning' | 'onsite'), initialized from `document.documentElement.getAttribute('data-mode')` or 'planning'
- On toggle: `document.documentElement.setAttribute('data-mode', newMode)` and `localStorage.setItem('kunsthaus-mode', newMode)`
- On mount: check localStorage for saved mode preference
- Render: `<button onClick={toggle} aria-pressed={mode === 'onsite'} class="hover-wipe-inverse touch-pulse touch-target">`
- Button text: `mode === 'planning' ? labels.planning[lang] : labels.onsite[lang]`
- Visual indicator: different background or icon for active state

**Create `src/components/ui/StickyCTA.astro`** per D-18, D-19, D-21:
- Props: `modeLabels` (from ui-strings), `ticketsLabel` (from ui-strings), `lang`, `ticketsHref: string` (default "https://www.kunsthaus.ch/tickets")
- **Desktop (1024px+, D-18):** `position: fixed`, `bottom: var(--space-8)`, `right: var(--space-8)`, `z-index: 50`
  - Two buttons in a vertical stack with `gap: var(--space-2)`:
    1. ModeToggle component (`client:load`)
    2. "Tickets" link: `<a href={ticketsHref} target="_blank" rel="noopener" class="hover-wipe-inverse touch-pulse touch-target">` with tickets label text
  - No containing bar -- just floating buttons with background color and padding
- **Mobile/Tablet (D-19):** `position: fixed`, `bottom: 0`, `left: 0`, `width: 100%`, `z-index: 50`
  - Horizontal bar with `background: var(--color-surface)`, `border-top: 1px solid var(--color-decorative)`
  - Flexbox row: "Tickets" left, ModeToggle right
  - Padding: `var(--space-3) var(--space-4)`
- Use `@media (min-width: 1024px)` and `@media (max-width: 1023px)` for responsive switching
- Both buttons use `.touch-target` for minimum 44x44px tap area

**Create `src/components/ui/SectionProgress.tsx`** per D-27 (React island):
- Props: `totalSections: number`, `sectionNames: string[]` (localized names)
- State: `currentSection` (number), `progress` (0-1 float)
- On mount: listen for custom event `sectionchange` dispatched by SectionWrapper's ScrollTrigger callbacks
  - Or: use GSAP ScrollTrigger directly within the React component to monitor `.stacking-section` elements
- Render: `position: fixed`, `bottom: var(--space-8)`, `left: var(--space-8)`, `z-index: 50`
  - Fraction counter: `<span>{String(currentSection + 1).padStart(2, '0')} / {String(totalSections).padStart(2, '0')}</span>` in `var(--type-sm)`, `var(--weight-regular)`
  - Progress arc: SVG circle, radius ~20px, `stroke-dasharray` and `stroke-dashoffset` computed from progress (0-1). Stroke: `var(--color-decorative-warm)` or `var(--color-text)`, 2px stroke width
  - Accessibility: `aria-label="Section {n} of {total}: {name}"`, `tabindex="0"`, `aria-live="polite"` on the counter container
- Hidden on mobile (`@media (max-width: 767px) { display: none }`) -- mobile uses scroll position naturally

**Create `src/components/ui/ReduceMotionToggle.astro`** per D-40:
- Simple toggle button/checkbox
- Label: "Reduce motion" / "Bewegung reduzieren" (from ui-strings or hardcoded fallback)
- On click: toggles `prefers-reduced-motion-override` class on `<html>` and stores in localStorage
- On load: checks localStorage and OS `prefers-reduced-motion` preference
- `<script>` reads localStorage key `kunsthaus-reduce-motion` and applies
- Styled: small, unobtrusive, placed in footer (and optionally in logo-island area per D-40)
- Add corresponding CSS in a `<style>` block:
  ```css
  html.reduce-motion *, html.reduce-motion *::before, html.reduce-motion *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
  ```
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && npx astro build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ModeToggle.tsx contains `data-mode` (sets attribute on html)
    - src/components/ui/ModeToggle.tsx contains `localStorage` (persists mode preference)
    - src/components/ui/ModeToggle.tsx contains `aria-pressed` (accessibility)
    - src/components/ui/StickyCTA.astro contains `z-index: 50` and `position: fixed`
    - src/components/ui/StickyCTA.astro contains `client:load` (ModeToggle island directive)
    - src/components/ui/StickyCTA.astro contains `target="_blank"` (tickets external link)
    - src/components/ui/StickyCTA.astro contains `@media` (responsive desktop/mobile layouts)
    - src/components/ui/SectionProgress.tsx contains `aria-live` and `aria-label`
    - src/components/ui/SectionProgress.tsx contains `stroke-dashoffset` or `strokeDashoffset` (SVG progress arc)
    - src/components/ui/SectionProgress.tsx contains `padStart` (fraction counter formatting)
    - src/components/ui/ReduceMotionToggle.astro contains `localStorage` and `reduce-motion`
    - `npx astro build` passes
  </acceptance_criteria>
  <done>Mode toggle switches data-mode between planning/onsite with localStorage persistence. Sticky CTA shows floating buttons (desktop) or bottom bar (mobile) with Tickets link and mode toggle. Section progress shows fraction counter and SVG arc, updated by scroll position. Reduce-motion toggle overrides all animations.</done>
</task>

</tasks>

<verification>
- SectionWrapper creates pinned stacking sections with correct z-index and background rotation
- CinematicReveal animates clipPath expansion on scroll
- PinnedNarrative pins and cycles through cards on desktop, stacks on mobile
- ExhibitionCard shows 60/40 image-text split
- Mode toggle changes data-mode attribute (semantic tokens switch automatically)
- Section progress displays fraction and arc
- Sticky CTA visible on all viewports
- `npx astro build` passes
</verification>

<success_criteria>
- HOME-02 complete: exhibitions displayed with Cinematic Reveal and Pinned Narrative
- HOME-03 complete: visit info, offerings, education rendered in appropriate section patterns
- Mode toggle functional, section progress tracking scroll position
- All content from JSON, all sections responsive
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-homepage/02-03-SUMMARY.md`
</output>
