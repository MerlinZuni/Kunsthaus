---
phase: 04-deploy-present
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nav/NavProfileButton.astro
  - src/components/nav/ProfileOverlay.astro
  - src/components/nav/AudioGuideOverlay.astro
autonomous: true
requirements:
  - TECH-05

must_haves:
  truths:
    - "Profile icon is an 18x18 inline SVG matching search/clock/hamburger icon style"
    - "Profile icon uses var(--color-text) and transparent background in both Planning and On-site modes"
    - "When profile overlay opens, the close X sits where the profile icon was in the navbar"
    - "Other nav icons (search, clock, hamburger) remain visible when profile overlay is open"
    - "Audio playlist shows exhibition title header above track list"
    - "Audio tracks have increased spacing, line dividers, and circular right-aligned play buttons"
    - "Audio detail player plays real audio from the MP3 file"
    - "Audio detail player fits within mobile 100dvh without scrolling"
  artifacts:
    - path: "src/components/nav/NavProfileButton.astro"
      provides: "Inline SVG profile icon with correct navbar styling"
      contains: "<svg width=\"18\" height=\"18\""
    - path: "src/components/nav/ProfileOverlay.astro"
      provides: "Close X aligned with navbar profile icon position"
      contains: "profile-overlay__close-x"
    - path: "src/components/nav/AudioGuideOverlay.astro"
      provides: "Polished playlist and working audio playback"
      contains: "audio-overlay__exhibition-title"
  key_links:
    - from: "src/components/nav/AudioGuideOverlay.astro"
      to: "public/audio/Ballad for Classical Strings.mp3"
      via: "audio.src with base path prefix"
      pattern: "\\$\\{base\\}\\$\\{track\\.audioSrc\\}"
---

<objective>
Visual polish for the profile button/overlay and audio guide components to bring them to presentation quality before screenshot capture.

Purpose: These components are visible in competition screenshots and the live demo. The profile icon must match the nav icon system, the profile overlay must follow the hamburger overlay navigation pattern, and the audio guide must look polished and play real audio.

Output: Polished NavProfileButton, ProfileOverlay, and AudioGuideOverlay ready for screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deploy-present/04-CONTEXT.md
@.planning/phases/04-deploy-present/04-RESEARCH.md

<interfaces>
<!-- NavBar icon pattern (18x18 stroke-based SVG) — from src/components/nav/NavBar.astro -->
```html
<svg width="18" height="18" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="2" stroke-linecap="round"
     stroke-linejoin="round" aria-hidden="true">
  <!-- icon paths -->
</svg>
```

<!-- NavBar right section layout — from NavBar.astro -->
```html
<div class="navbar__right">
  <NavProfileButton />
  <button class="navbar__icon-btn touch-target touch-pulse" aria-label={menuLabel} data-action="hamburger">
    <!-- hamburger SVG -->
  </button>
</div>
```

<!-- HamburgerOverlay close X pattern — from HamburgerOverlay.astro lines 49-54 -->
```html
<button class="hamburger-overlay__close touch-target" aria-label="Close menu">
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
       stroke-width="2" stroke-linecap="round" aria-hidden="true">
    <line x1="18" y1="6" x2="6" y2="18"/>
    <line x1="6" y1="6" x2="18" y2="18"/>
  </svg>
</button>
```

<!-- Audio overlay base path — from AudioGuideOverlay.astro line 13 -->
```typescript
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
```
<!-- JS base path — from AudioGuideOverlay.astro line 394 -->
```typescript
const base = (window as any).__audioGuideBase || '';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile icon SVG and profile overlay close-X alignment</name>
  <files>src/components/nav/NavProfileButton.astro, src/components/nav/ProfileOverlay.astro</files>
  <read_first>
    - src/components/nav/NavProfileButton.astro (current Material Symbol icon to replace)
    - src/components/nav/ProfileOverlay.astro (current close-X positioning to fix)
    - src/components/nav/NavBar.astro (reference: inline SVG icon style, navbar__right layout, navbar__icon-btn class)
    - src/components/nav/HamburgerOverlay.astro (reference: close X replaces trigger pattern, header mirroring navbar)
  </read_first>
  <action>
**NavProfileButton.astro (per D-03, D-04):**

1. Replace the `<span class="icon" aria-hidden="true">account_circle</span>` (line 14) with an inline SVG:
```html
<svg width="18" height="18" viewBox="0 0 24 24" fill="none"
     stroke="currentColor" stroke-width="2" stroke-linecap="round"
     stroke-linejoin="round" aria-hidden="true">
  <circle cx="12" cy="8" r="4"/>
  <path d="M20 21c0-3.87-3.58-7-8-7s-8 3.13-8 7"/>
</svg>
```

2. Remove the `.navbar__profile-btn .icon { font-size: 24px; }` style rule (line 24-26) since the SVG is self-sizing at 18x18.

3. Ensure the button class list includes `navbar__icon-btn` (already present on line 9) which provides `color: var(--color-text)` and transparent background. Do NOT add any hardcoded `background: white` or `color: white`.

**ProfileOverlay.astro (per D-05, D-06):**

4. Restructure the `.profile-overlay__header` to mirror the navbar layout. The close X must be positioned in the `navbar__right` position (right side of the header, same padding as NavBar). Change the header to:
```html
<header class="profile-overlay__header">
  <div class="profile-overlay__header-left">
    <button type="button" class="profile-overlay__back touch-target" data-profile-close>
      <span class="icon" aria-hidden="true">arrow_back</span>
      <span>Profile</span>
    </button>
  </div>
  <div class="profile-overlay__header-right">
    <button type="button" class="profile-overlay__close-x navbar__icon-btn touch-target touch-pulse" data-profile-close-x aria-label="Close">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor"
           stroke-width="2" stroke-linecap="round" aria-hidden="true">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>
</header>
```

5. Update `.profile-overlay__header` CSS to match NavBar spacing:
```css
.profile-overlay__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4);
  flex-shrink: 0;
}
.profile-overlay__header-right {
  display: flex;
  align-items: center;
  gap: var(--space-3);
}
```

6. Remove the old `.profile-overlay__close-x .icon { font-size: 22px; }` rule since the close X is now an inline SVG.

7. In the ProfileOverlay's open/close JS, when the overlay opens, hide the `#nav-profile-btn` element (`display: none`). When the overlay closes, restore it (`display: flex` if logged in). This ensures the close X visually replaces the profile icon.
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && grep -c 'svg width="18" height="18"' src/components/nav/NavProfileButton.astro && grep -c 'account_circle' src/components/nav/NavProfileButton.astro && grep -c 'profile-overlay__header-right' src/components/nav/ProfileOverlay.astro</automated>
  </verify>
  <acceptance_criteria>
    - NavProfileButton.astro contains `<svg width="18" height="18" viewBox="0 0 24 24"` (inline SVG, not Material Symbol)
    - NavProfileButton.astro does NOT contain `account_circle`
    - NavProfileButton.astro does NOT contain `font-size: 24px`
    - NavProfileButton.astro does NOT contain `background: white` or `background-color: white`
    - ProfileOverlay.astro contains `profile-overlay__header-right` div wrapping the close X
    - ProfileOverlay.astro close X uses `<svg width="18" height="18"` (inline SVG X mark), not Material Symbol `close`
    - ProfileOverlay.astro JS hides `#nav-profile-btn` on open and restores on close
  </acceptance_criteria>
  <done>Profile icon is a stroke-based 18x18 SVG matching other nav icons. Profile overlay close X appears at the navbar profile icon position. Other nav icons remain visible when profile overlay is open.</done>
</task>

<task type="auto">
  <name>Task 2: Audio playlist styling and audio playback wiring</name>
  <files>src/components/nav/AudioGuideOverlay.astro</files>
  <read_first>
    - src/components/nav/AudioGuideOverlay.astro (full file — playlist HTML, CSS, and JS including loadTrack function at line 438-458)
    - src/content/onsite/kerry-james-marshall.json (audioSrc paths, exhibition title)
    - src/styles/tokens.css (CSS custom properties for spacing, colors)
  </read_first>
  <action>
**Playlist styling (per D-07, D-08, D-09, D-10, D-11):**

1. Add an exhibition title header in the `buildPlaylist()` JS function (around line 411). Before appending tracks, insert a header element:
```javascript
// At the start of buildPlaylist(), after clearing tracksList:
const headerEl = document.createElement('h2');
headerEl.className = 'audio-overlay__exhibition-title';
headerEl.textContent = (window as any).__audioGuideExhibitionTitle || 'Audio Guide';
tracksList.appendChild(headerEl);
```

Also, in the overlay's `open()` function, accept and store the exhibition title. In the `AudioGuideOverlay` initialization script, extract the exhibition title from the data when `window.__audioGuideOverlay.open(trackId)` is called — the exhibition data from `kerry-james-marshall.json` includes the exhibition `title` field. Set it on `window.__audioGuideExhibitionTitle`.

2. Add CSS for the exhibition title header:
```css
.audio-overlay__exhibition-title {
  font-size: var(--type-lg);
  font-weight: var(--weight-medium);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-text);
  padding: var(--space-4) var(--space-2) var(--space-2);
  margin: 0;
}
```

3. Increase track vertical spacing — change `.audio-overlay__track` padding from `var(--space-4) var(--space-2)` to `var(--space-5) var(--space-3)` (more vertical breathing room per D-08).

4. The existing `.audio-overlay__track` already has `border-bottom: 1px solid color-mix(...)` (line 146), which provides line dividers per D-09. Verify this remains.

5. Make play buttons circular and right-aligned (per D-10). Update the `.audio-overlay__track-play` CSS:
```css
.audio-overlay__track-play {
  font-size: 20px;
  opacity: 0.6;
  transition: opacity 0.2s ease;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  border: 1px solid color-mix(in srgb, var(--color-text) 25%, transparent);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-left: auto;
}
.audio-overlay__track:hover .audio-overlay__track-play {
  opacity: 1;
  border-color: color-mix(in srgb, var(--color-text) 50%, transparent);
}
```

**Audio playback wiring (per D-12):**

6. Fix the audio source path in `loadTrack()` function (around line 444). Change:
```javascript
audio.src = track.audioSrc;
```
to:
```javascript
audio.src = `${base}${track.audioSrc}`;
```
This matches the image pattern on line 448 and fixes the 404 on GitHub Pages where the base is `/Kunsthaus`.

**Detail player mobile fit (per D-13):**

7. Update `.audio-overlay__detail` CSS to ensure it fits within `100dvh`. Add:
```css
.audio-overlay__detail-content {
  display: flex;
  flex-direction: column;
  height: calc(100dvh - 48px); /* minus handle height */
  overflow: hidden;
}

.audio-overlay__detail-figure {
  flex: 1;
  min-height: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}

.audio-overlay__detail-image {
  max-height: 100%;
  max-width: 100%;
  object-fit: contain;
}
```

8. Constrain the text content below the image. Set max-height or limit the description to 2-3 lines with `-webkit-line-clamp: 3` to prevent overflow:
```css
.audio-overlay__detail-description {
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
}
```
  </action>
  <verify>
    <automated>cd /Users/merlinzuni/develop/Kunsthaus && grep -c 'audio-overlay__exhibition-title' src/components/nav/AudioGuideOverlay.astro && grep 'audio.src' src/components/nav/AudioGuideOverlay.astro | head -3 && grep -c 'border-radius: 50%' src/components/nav/AudioGuideOverlay.astro</automated>
  </verify>
  <acceptance_criteria>
    - AudioGuideOverlay.astro contains `audio-overlay__exhibition-title` class (at least 2 occurrences: JS + CSS)
    - AudioGuideOverlay.astro `loadTrack` function sets `audio.src = \`${base}${track.audioSrc}\`` (NOT bare `track.audioSrc`)
    - AudioGuideOverlay.astro contains `border-radius: 50%` for circular play buttons
    - AudioGuideOverlay.astro track padding uses `var(--space-5)` (increased from `var(--space-4)`)
    - AudioGuideOverlay.astro detail content uses `100dvh` constraint
    - AudioGuideOverlay.astro contains `-webkit-line-clamp: 3` for description overflow
  </acceptance_criteria>
  <done>Audio playlist has an exhibition title header, increased track spacing, circular play buttons. Audio plays real MP3 via correct base-prefixed path. Detail player fits within mobile viewport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audio.src path | User-controlled content (from JSON) used as audio source path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | audio.src from JSON | accept | Audio source comes from bundled JSON, not user input. No server-side processing. Static prototype with no user-submitted data. |
| T-04-02 | I (Info Disclosure) | Profile overlay | accept | Demo user data is hardcoded mock content with no real PII. Static prototype. |
</threat_model>

<verification>
1. `npm run build` completes without errors
2. Profile icon renders as SVG (not Material Symbol glyph) in both modes
3. Audio plays when tapping a track in the audio guide
4. Detail player fits within viewport on mobile (375px width)
</verification>

<success_criteria>
- Profile icon is an inline SVG matching the nav icon system
- Profile overlay close X replaces the profile icon in the navbar position
- Audio playlist shows exhibition title, has proper spacing and circular play buttons
- Audio detail player plays actual audio from the MP3 file
- All components render correctly in both Planning and On-site modes
</success_criteria>

<output>
After completion, create `.planning/phases/04-deploy-present/04-01-SUMMARY.md`
</output>
